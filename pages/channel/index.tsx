import React from 'react';
import Workspace from '@layouts/workspace/Workspace';

const Channel = () => {
  // @ts-ignore
  return (
    <Workspace>
      채널!
    </Workspace>
  );
};

export default Channel;
